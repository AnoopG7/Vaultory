import { Store } from 'lucide-react'
import { PagePlaceholder } from '@/components/layout'

export default function StoresPage() {
  return (
    <PagePlaceholder
      icon={Store}
      title="Stores"
      description="Manage the three locations, their details, and owner-level settings."
    />
  )
}
